!!!
%html{html_attrs('en-us')}
  %head
    %meta{content: "text/html; charset=utf-8", "http-equiv": "Content-Type"}
    - if Rails.env.production?
      = stylesheet_pack_tag 'mailer', type: 'text/css', media: 'all'
    - else
      = javascript_pack_tag 'mailer'
  %body.bg-light
    .container.mt-2
      %a{href: root_url}
        %img.img-system-logo.mb-2{src: asset_url('/SJWlogo.svg')}
      = yield
      %hr
      %p.text-center
        = link_to ClientInfo.owner, EnvironmentInfo.env_url
        %br= ClientInfo.address_1
        #{ClientInfo.city}, #{ClientInfo.state} #{ClientInfo.zip_code}
        %br
        Find us on
        %a{href: ClientInfo.facebook_link} Facebook
        and
        %a{href: ClientInfo.instagram_link} Instagram
