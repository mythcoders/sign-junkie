- page_title 'Invoice Search'

.card
  %h5.card-header.bg-primary.text-white
    = fa_regular 'shopping-bag'
    Invoices
  %table.table.table-bordered.table-striped.table-border-fix
    %thead
      %tr
        %th Number
        %th Customer
        %th Date
        %th Status
    %tbody
      - @invoices.each do |o|
        %tr{data: { 'clickable-row': '', 'href': admin_invoice_path(o) }}
          %td= o.order_number
          %td= o.customer.full_name
          %td= lifp(o.date_created, :short)
          %td= o.status
  .card-footer
    = paginate @invoices
    = page_entries_info @invoices
