- page_title 'New', 'Event Image'

= form_for :event, url: admin_event_images_path, local: true do |f|
  .card
    %h5.card-header.bg-primary.text-white
      = "New Image for #{@event.name}"
    .card-body
      = error_messages_for 'event'
      = f.hidden_field :id
      .form-group
        .custom-file
          = f.file_field :images, multiple: true, class: 'custom-file-input'
          = f.label :select_one_or_more_images_to_upload, class: 'custom-file-label'
    .card-footer
      = link_to 'Cancel', admin_event_path(@event), class: 'btn btn-outline-secondary'
      = f.submit 'Upload', class: 'btn btn-success'