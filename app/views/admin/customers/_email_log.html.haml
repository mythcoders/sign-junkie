.card.mt-3
  - unless emails.any?
    .card-body.text-center
      %h2 No Emails
      %p.text-muted.mb-0 Any emails sent to this customers will appear here.
  - else
    .table-responsive.mb-0
      %table.table.table-sm.table-nowrap.card-table.table-hover
        %thead
          %tr
            %th Date Sent
            %th Subject
        %tbody
          - emails.each do |email|
            %tr
              %td= l email.created_at
              %td= email.subject

