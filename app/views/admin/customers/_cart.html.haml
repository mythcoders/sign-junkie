.card.mt-3
  - unless carts.any?
    .card-body.text-center
      %h2 Empty Cart
      %p.text-muted.mb-0 Any items in this customers cart will appear here.
  - else
    .table-responsive.mb-0
      %table.table.table-sm.table-nowrap.card-table.table-hover
        %thead
          %tr
            %th Item
            %th Price
            %th Date Added
        %tbody
          - carts.each do |cart|
            %tr
              %td= render(ItemDescriptionDisplay::Component.admin(cart))
              %td= number_to_currency cart.line_total
              %td= l cart.created_at
