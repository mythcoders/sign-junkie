- if Rails.env.production?
  .card
    .card-header
      Coming Soon!
    %img.card-img-top{src: asset_path('cover')}
    .card-body
      %h5.card-title Thanks for stopping by
      %p.card-text
        We're in the process of setting everything up so check back soon!
        In the meantime, please visit our Facebook page to stay updated.
      %a.btn.btn-primary{href: 'https://www.facebook.com/signjunkieworkshop/'}
        = fa_brands 'facebook'
        Facebook
- else
  .row
    .col-3
      = render partial: 'events/filters'
    .col-9
      - if @events.any?
        = render partial: "events/#{@view}"
      - else
        %h3 Looks like we didn't have any events that matched that criteria.
