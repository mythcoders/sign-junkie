{{- if .Values.service.enabled -}}
Application should be accessible at: {{ template "hostname" $.Values.service.url }}
{{- else -}}
Application will be accessible at: {{ template "hostname" $.Values.service.url }} when you deploy stable track.
{{- end -}}
